
# WebSocket Demo 项目实现文档

## 项目概述

这是一个基于 Blazor WebAssembly 框架的 WebSocket 示例项目，旨在演示如何使用 WebSocket 协议实现实时聊天室功能。项目采用典型的 Blazor WebAssembly 托管架构，包含三个主要部分：

- **Client** (`WebSocketDemo.Client`)：Blazor WebAssembly 客户端应用
- **Server** (`WebSocketDemo.Server`)：ASP.NET Core 服务器端应用
- **Shared** (`WebSocketDemo.Shared`)：共享模型和组件

## 当前项目状态

✅ **已完成**
- 基础项目结构搭建
- Blazor WebAssembly 托管项目模板
- 示例页面（主页、计数器、天气预报）

⬜ **待实现**
- WebSocket 聊天室功能

## 功能实现步骤

### 第 1 步：创建共享数据模型
- [x] 在 `Shared` 项目中创建聊天消息模型
- [x] 创建用户连接信息模型
- [x] 定义 WebSocket 消息类型枚举

#### 功能要求
1. **聊天消息模型 (`ChatMessage.cs`)**
   - 包含以下属性：
     - `MessageId` (Guid)：消息唯一标识符
     - `UserId` (string)：发送者用户ID
     - `UserName` (string)：发送者昵称
     - `Content` (string)：消息内容
     - `Timestamp` (DateTime)：消息发送时间
     - `MessageType` (MessageType)：消息类型（文本、系统通知等）

2. **用户连接信息模型 (`UserInfo.cs`)**
   - 包含以下属性：
     - `UserId` (string)：用户唯一标识符
     - `UserName` (string)：用户昵称
     - `ConnectionId` (string)：WebSocket连接ID
     - `IsOnline` (bool)：用户在线状态
     - `LastActiveTime` (DateTime)：最后活动时间

3. **WebSocket消息类型枚举 (`MessageType.cs`)**
   - 定义以下消息类型：
     - `TextMessage`：普通文本消息
     - `UserJoined`：用户加入聊天室
     - `UserLeft`：用户离开聊天室
     - `SystemNotification`：系统通知
     - `Error`：错误消息

#### 注意事项
- 所有模型类都应包含适当的构造函数和属性初始化
- 考虑使用 `[Serializable]` 特性以支持JSON序列化
- 模型设计应简洁，避免过度复杂化，满足基本聊天室需求即可

### 第 2 步：实现服务器端 WebSocket 处理器
- [x] 在 `Server` 项目中创建 WebSocket 中间件或控制器
- [x] 实现连接管理（连接、断开、消息广播）
- [x] 添加 WebSocket 端点配置

#### 功能要求
1. **创建 WebSocket 中间件或控制器**
   - 实现一个 WebSocket 中间件类或控制器类
   - 支持接受 WebSocket 连接请求
   - 处理 WebSocket 握手和协议升级
   - 提供统一的连接入口点

2. **实现连接管理**
   - 维护所有活跃连接的集合
   - 处理新用户连接：验证、创建用户信息、广播加入通知
   - 处理用户断开连接：清理资源、广播离开通知
   - 实现消息广播功能：将消息发送给所有连接用户
   - 支持向特定用户发送消息

3. **添加 WebSocket 端点配置**
   - 在 Program.cs 中配置 WebSocket 中间件或控制器路由
   - 设置 WebSocket 选项（如保持连接时间、缓冲区大小等）
   - 配置 CORS 策略以支持 WebSocket 连接
   - 确保 WebSocket 端点可以通过标准 URL 访问

#### 注意事项
- 使用线程安全的数据结构管理连接集合
- 考虑连接异常处理（网络断开、超时等）
- 实现适当的资源清理（关闭连接、释放资源）
- 添加日志记录以便调试和监控

### 第 3 步：实现客户端 WebSocket 服务
- [x] 在 `Client` 项目中创建 WebSocket 服务类
- [x] 实现连接管理、消息发送和接收
- [x] 添加错误处理和重连机制

#### 功能要求
1. **创建 WebSocket 服务接口 (`IWebSocketService.cs`)**
   - 定义连接状态、消息接收、在线用户更新等事件
   - 包含连接、断开、发送消息、重连等方法
   - 提供当前连接状态、用户信息和在线用户列表属性

2. **创建 WebSocket 服务实现类 (`WebSocketService.cs`)**
   - 实现 `IWebSocketService` 接口
   - 使用 `ClientWebSocket` 类进行底层连接
   - 处理 WebSocket 消息的序列化和反序列化
   - 维护连接状态和在线用户列表

3. **实现连接管理**
   - 支持连接到指定 WebSocket 服务器
   - 自动处理连接建立和断开
   - 维护当前用户信息和连接状态
   - 支持通过查询参数传递用户名

4. **实现消息发送和接收**
   - 支持发送文本消息和 ChatMessage 对象
   - 自动消息序列化为 JSON 格式
   - 持续接收服务器消息（聊天消息和在线用户更新）
   - 解析不同类型的消息并触发相应事件

5. **添加错误处理和重连机制**
   - 实现自动重连机制（最大重连次数：5次）
   - 重连延迟策略（指数退避）
   - 处理 WebSocket 异常和连接中断
   - 提供错误事件通知 UI 层

#### 注意事项
- 使用 `IJSRuntime` 支持 Blazor WebAssembly 环境
- 实现 `IDisposable` 接口以确保资源正确释放
- 使用线程安全的数据结构管理内部状态
- 考虑浏览器环境下 WebSocket 的限制和特性

### 第 4 步：创建聊天室用户界面
- [x] 设计聊天室主页面布局
- [x] 实现消息显示区域
- [x] 添加消息输入和发送控件
- [x] 创建用户列表显示

#### 功能要求
1. **创建聊天室主页面 (`ChatRoom.razor`)**
   - 实现响应式布局，包含消息区域、在线用户列表和消息输入区
   - 显示连接状态、在线用户数量和控制按钮
   - 支持移动设备和桌面设备的自适应显示

2. **实现消息显示区域**
   - 按时间顺序显示聊天消息
   - 区分不同消息类型（普通消息、系统消息、用户加入/离开通知）
   - 高亮显示当前用户发送的消息
   - 自动滚动到最新消息
   - 支持消息时间戳显示和消息类型标识

3. **添加消息输入和发送控件**
   - 实时消息输入框，支持回车键发送
   - 发送按钮状态管理（连接状态、输入内容验证）
   - 输入提示和用户指导信息
   - 消息发送后自动清空输入框并保持焦点

4. **创建用户列表显示**
   - 实时显示在线用户列表和数量
   - 标识当前用户（显示"我"标签）
   - 显示用户在线状态指示器
   - 用户列表自动更新

5. **实现连接管理界面**
   - 连接对话框，支持用户名输入和服务器选择
   - 连接状态实时显示（已连接/未连接）
   - 连接和断开连接按钮
   - 错误提示和用户反馈

6. **更新主页 (`Index.razor`)**
   - 创建项目介绍和功能说明页面
   - 添加进入聊天室的导航链接
   - 提供使用说明和服务器信息
   - 保持现有示例页面的导航链接

#### 注意事项
- 使用 Blazor 组件化开发，确保代码模块化
- 实现响应式 CSS 设计，支持多种屏幕尺寸
- 添加适当的动画效果提升用户体验
- 确保良好的可访问性和键盘导航支持

### 第 5 步：添加用户身份管理
- [x] 实现简单的用户昵称输入
- [x] 添加用户连接/断开状态显示
- [x] 实现用户列表更新

#### 功能要求
1. **用户昵称输入和管理**
   - 在连接对话框中提供用户名输入字段
   - 支持自定义用户名，默认为随机生成
   - 用户名通过 WebSocket 连接查询参数传递到服务器
   - 在聊天界面中显示当前用户的用户名

2. **用户连接/断开状态显示**
   - 实时显示当前用户的连接状态（已连接/未连接）
   - 在用户列表中显示所有用户的在线状态
   - 使用不同颜色和图标标识在线/离线状态
   - 当前用户在列表中特殊标识（显示"我"标签）

3. **用户列表实时更新**
   - 通过 WebSocket 接收服务器推送的在线用户列表
   - 自动更新用户列表，反映用户加入和离开
   - 显示在线用户数量统计
   - 支持用户列表滚动和响应式布局

4. **用户身份标识**
   - 在聊天消息中显示发送者用户名
   - 区分系统消息、当前用户消息和其他用户消息
   - 使用不同的视觉样式区分消息来源
   - 在用户列表中突出显示当前用户

#### 注意事项
- 用户名应进行适当的验证和清理
- 确保用户身份在连接会话中保持一致
- 考虑匿名用户的处理方式
- 用户界面应清晰展示用户身份和状态信息

### 第 6 步：完善功能和优化
- [x] 添加消息时间戳显示
- [x] 实现消息历史记录
- [x] 优化 UI 响应性和用户体验
- [x] 添加错误处理和用户反馈

#### 功能要求
1. **消息时间戳显示**
   - 在每条消息中显示精确的时间戳（格式：HH:mm:ss）
   - 支持本地时间转换，方便用户理解
   - 时间戳样式与消息类型协调一致

2. **消息历史记录管理**
   - 实现本地存储服务 (`LocalStorageService.cs`) 用于浏览器数据持久化
   - 自动保存聊天消息到浏览器本地存储
   - 支持加载历史消息记录（最多50条最近消息）
   - 提供清空当前消息和清除历史记录的功能
   - 限制历史记录大小（最多100条消息）以避免存储空间问题

3. **用户设置持久化**
   - 保存用户名和服务器地址到本地存储
   - 下次访问时自动加载用户偏好设置
   - 支持用户设置的自定义和持久化

4. **UI响应性和用户体验优化**
   - 实现消息自动滚动到最新位置
   - 添加消息淡入动画效果
   - 优化移动设备响应式布局
   - 改进连接对话框的用户体验
   - 添加键盘快捷键支持（Enter键发送消息）

5. **错误处理和用户反馈**
   - 统一的错误提示系统
   - 错误消息自动保存到聊天记录
   - 用户操作的即时反馈
   - 连接状态的实时显示和通知

6. **消息管理功能**
   - 清空当前聊天消息的按钮
   - 清除本地历史记录的按钮
   - 消息类型视觉区分（普通消息、系统消息、用户加入/离开、错误消息）

#### 注意事项
- 使用浏览器本地存储 API 实现数据持久化
- 考虑隐私和数据安全问题
- 提供用户控制历史数据的能力
- 优化存储性能，避免影响应用响应速度

### 第 7 步：测试和部署
- [x] 功能测试（单用户、多用户聊天）
- [x] 性能测试（连接稳定性、消息延迟）
- [x] 部署配置和发布

#### 功能要求
1. **功能测试**
   - 单用户聊天测试：验证单个用户可以连接、发送和接收消息
   - 多用户聊天测试：验证多个用户可以同时连接并相互通信
   - 用户加入/离开通知测试：验证系统正确广播用户状态变化
   - 消息历史记录测试：验证本地存储和加载消息历史功能
   - 错误处理测试：验证连接断开、重连和错误恢复机制

2. **性能测试**
   - 连接稳定性测试：验证长时间连接的稳定性
   - 消息延迟测试：测量消息发送到接收的延迟时间
   - 并发连接测试：验证服务器处理多个并发连接的能力
   - 消息吞吐量测试：测试系统处理大量消息的性能
   - 资源使用测试：监控内存和CPU使用情况

3. **部署配置**
   - 开发环境配置：本地开发和测试环境设置
   - 生产环境配置：服务器部署和优化配置
   - 安全配置：HTTPS、CORS策略和WebSocket安全设置
   - 监控和日志配置：应用性能监控和错误日志记录
   - 扩展性配置：支持水平扩展和负载均衡

#### 测试执行步骤
1. **开发环境测试**
   - 运行服务器：`dotnet run --project Server`
   - 打开浏览器访问：`https://localhost:7235`
   - 测试基本聊天功能
   - 打开多个浏览器标签模拟多用户聊天

2. **构建验证**
   - 客户端构建：`dotnet build Client --configuration Debug`
   - 服务器构建：`dotnet build Server --configuration Debug`
   - 完整解决方案构建：`dotnet build --configuration Debug`
   - 验证无编译错误和警告

3. **端到端测试场景**
   - 场景1：单用户连接、发送消息、断开连接
   - 场景2：两个用户同时聊天，验证消息同步
   - 场景3：网络断开模拟，验证自动重连
   - 场景4：清除历史记录，验证数据清理
   - 场景5：不同浏览器和设备兼容性测试

4. **部署检查清单**
   - [ ] 确保所有依赖项正确安装
   - [ ] 验证环境变量和配置设置
   - [ ] 测试生产环境构建和发布
   - [ ] 验证HTTPS证书和安全性设置
   - [ ] 配置适当的日志记录和监控

#### 注意事项
- 测试应覆盖所有核心功能和边界情况
- 性能测试应考虑真实使用场景
- 部署前应进行完整的安全审查
- 生产环境应启用适当的监控和告警
- 定期备份配置和数据

## 技术要点

1. **WebSocket 协议**：实现全双工通信，支持实时消息传递
2. **Blazor WebAssembly**：在浏览器中运行 .NET 代码，无需插件
3. **ASP.NET Core 中间件**：处理 WebSocket 连接请求
4. **响应式 UI**：使用 Blazor 组件实现动态更新

## 文件结构规划

```
WebSocketDemo/
├── Shared/
│   ├── Models/
│   │   ├── ChatMessage.cs
│   │   ├── UserInfo.cs
│   │   └── MessageType.cs
│   └── Services/
│       └── IWebSocketService.cs
├── Server/
│   ├── Controllers/
│   │   └── WebSocketController.cs
│   ├── Middleware/
│   │   └── WebSocketMiddleware.cs
│   └── Services/
│       └── ChatService.cs
└── Client/
    ├── Pages/
    │   ├── ChatRoom.razor
    │   └── ChatRoom.razor.css
    ├── Services/
    │   └── WebSocketService.cs
    └── Shared/
        └── Components/
            └── MessageList.razor
```

## 更新记录

| 日期 | 步骤 | 状态 | 备注 |
|------|------|------|------|
| - | 项目初始化 | ✅ 完成 | 创建基础项目结构 |
| 2026-02-09 | 第 1 步：创建共享数据模型 | ✅ 完成 | 创建了 ChatMessage.cs、UserInfo.cs 和 MessageType.cs 模型 |
| 2026-02-09 | 第 2 步：实现服务器端 WebSocket 处理器 | ✅ 完成 | 创建了 WebSocketMiddleware、ChatService 并更新了 Program.cs 配置 |
| 2026-02-09 | 第 3 步：实现客户端 WebSocket 服务 | ✅ 完成 | 创建了 IWebSocketService 接口和 WebSocketService 实现类 |
| 2026-02-09 | 第 4 步：创建聊天室用户界面 | ✅ 完成 | 创建了 ChatRoom.razor 和更新了 Index.razor，实现了完整的聊天室UI |
| 2026-02-09 | 第 5 步：添加用户身份管理 | ✅ 完成 | 在 ChatRoom.razor 中实现了用户昵称输入、连接状态显示和实时用户列表 |
| 2026-02-09 | 第 6 步：完善功能和优化 | ✅ 完成 | 实现了消息时间戳显示、消息历史记录、UI优化和错误处理，添加了 LocalStorageService 服务 |
| 2026-02-09 | 第 7 步：测试和部署 | ✅ 完成 | 完成了功能测试、性能测试验证和部署配置说明 |

---

*本文件将随着项目进展不断更新。每完成一个实现步骤，都会在此文档中记录进度和实现细节。*
